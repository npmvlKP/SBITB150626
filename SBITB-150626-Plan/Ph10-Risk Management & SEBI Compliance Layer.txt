# Phase 10: Risk Management & SEBI Compliance Layer — Sequential AI-Agent Implementation PLAN

> **Duration:** Weeks 21-24 | **Output:** Extend `indian-trading-bot/src/risk/` with quantitative risk engines, 15 pre-trade checks, graduated kill switch, dual-write audit trail
> **Environment:** Windows 11 + Python 3.11+ + Docker (WSL2 backend)
> **Reference Books:** McNeil/Frey/Embrechts "Quantitative Risk Management" (Revised Ed), NISM "Indian Securities Market: SEBI & Regulatory Framework"
> **Reference Docs:** `SEBI_Algo_Trading_Regulation_Research.md` + `COMPLIANCE_SECURITY_MAPPING.md` + `tooling-reference.md`
> **Extends:** Phase 0 scaffolding (`PHASE_0_INSTRUCTIONS.md`) — does NOT overwrite existing files
> **CRITICAL:** This is a PLAN ONLY — no implementation, no other activity

---

## Critical Corrections Carried Forward from Phase 0

1. **500ms resting time is NOT a SEBI mandate** — proposed 2016, dropped 2018 (SEBI/HO/MRD/DP/CIR/P/2018/62). NO constant, NO check, NO reference.
2. **Algo ID tagging is BROKER's responsibility** — per SEBI/HO/MIRSD/MIRSD-PoD/P/CIR/2025/0000013, the exchange provides unique algo ID; broker tags orders server-side. Our `tag` field = our own audit trail/attribution only.
3. **OPS threshold = 10** per NSE/INVG/67858 (May 5, 2025) — below this, no registration needed; above, must register algo with exchange through broker.
4. **Zerodha has NO sandbox/test environment** — paper trading mode logs orders but doesn't send.
5. **Bracket Orders disabled on Zerodha since 2021** — trailing stop must be implemented in bot logic + SL-M orders.
6. **Position limits (5 NIFTY, 3 BANKNIFTY) are self-imposed** — NOT SEBI mandates. SEBI mandates exposure limits at individual client level (CIR/MRD/DP/09/2012) but does not prescribe specific lot counts per instrument. These are our own risk parameters.
7. **Self-trade prevention is client-side addition** — Zerodha RMS is primary enforcement; we add client-side check as defense-in-depth.
8. **Order modification limit = 25 per order** — per Zerodha API docs. This replaces the erroneous "500ms minimum resting time" check from Plan.txt Phase 10 draft.

---

## Phase 0 Scaffolding Already Built (DO NOT recreate — EXTEND only)

### Existing Files:
- `src/risk/compliance.py` — ComplianceLevel enum, Segments enum, TradingSession enum, get_trading_session(), is_order_allowed(), check_ops_threshold(), validate_symbol(), format_algo_tag(), SEBI_CIRCULAR_REFERENCES
- `src/risk/kill_switch.py` — KillSwitchLevel enum (INACTIVE|THROTTLE|PAUSE|KILL), KillSwitchEvent dataclass, KillSwitch class with 3 activation paths (keyboard, telegram, rest_api), register_keyboard_kill_switch(), KillSwitchAPI, KillSwitchTelegramHandler
- `src/risk/manager.py` — RiskCheckResult enum, PreTradeCheckDetail/PreTradeCheckResult dataclasses, RiskManager class with 10 pre-trade checks, TokenBucketRateLimiter, check_daily_loss(), check_margin_utilization(), check_rejection_rate()
- `src/risk/audit.py` — AuditEvent dataclass, AuditLogger class (SHA-256 checksums, NTP-synchronized timestamps), NTPClock class, audit event type constants
- `config/settings.py` — ComplianceSettings, RiskSettings, KillSwitchSettings, AuditSettings, BrokerSettings (all Pydantic BaseSettings)
- `deployment/init.sql` — audit_events hypertable with append-only triggers, 7-year retention, indexes
- `tests/risk/test_compliance.py`, `tests/risk/test_kill_switch.py`, `tests/risk/test_manager.py`, `tests/risk/test_audit.py` — Phase 0 test suite
- `tests/conftest.py` — shared fixtures

### What Phase 10 Adds (NEW files/classes):
- `src/risk/var_engine.py` — VaR/CVaR/GARCH/EVT quantitative risk engines
- `src/risk/self_trade_prevention.py` — Client-side self-trade detection
- `src/risk/order_validator.py` — Order modification limit, advanced order validation
- `src/risk/risk_monitor.py` — Real-time risk monitoring dashboard metrics
- `src/risk/risk_api.py` — FastAPI REST endpoints for risk controls
- `config/settings.py` — ADD QuantitativeRiskSettings, PositionLimitSettings (extend, don't overwrite)
- `deployment/init.sql` — ADD order_audit_trail, risk_audit_trail hypertables (extend, don't overwrite)
- New test files for unit/property/replay/integration/bench/load tests

---

## New Dependencies (add to pyproject.toml [project] dependencies)

```
"scipy>=1.12",       # Statistical functions for VaR/GARCH (McNeil Ch.2-5)
"statsmodels>=0.14", # GARCH models, ACF/PACF diagnostics (McNeil Ch.4)
"arch>=7.0",         # ARCH/GARCH volatility models (McNeil Ch.4)
"fastapi>=0.110",    # Risk API REST endpoints (Phase 10.12)
"uvicorn>=0.29",     # ASGI server for FastAPI
```

These are ADDITIONAL to existing Phase 0 dependencies. Do NOT remove any existing dependency.

---

## Instruction 1 — Add `QuantitativeRiskSettings` to `config/settings.py`

**Action:** EXTEND `config/settings.py` — add two new Pydantic BaseSettings classes AFTER existing classes. Do NOT modify any existing class.

### 1.1 QuantitativeRiskSettings

```python
class QuantitativeRiskSettings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="QUANT_RISK_", env_file=".env", extra="ignore")

    VAR_CONFIDENCE_LEVEL: float = Field(default=0.95, ge=0.90, le=0.999)
    VAR_LOOKBACK_DAYS: int = Field(default=252, ge=30, le=1260)
    VAR_HOLDING_PERIOD_DAYS: int = Field(default=1, ge=1, le=10)
    VAR_METHOD: str = Field(default="historical", pattern="^(historical|parametric|monte_carlo)$")
    CVAR_CONFIDENCE_LEVEL: float = Field(default=0.975, ge=0.90, le=0.999)
    VAR_MAX_PORTFOLIO_VAR: Decimal = Field(default=Decimal("100000"))  # Rs 1L max daily VaR
    VAR_MAX_PORTFOLIO_CVAR: Decimal = Field(default=Decimal("150000"))  # Rs 1.5L max daily CVaR
    GARCH_MODEL_TYPE: str = Field(default="GARCH", pattern="^(GARCH|EGARCH|GJR)$")
    GARCH_P: int = Field(default=1, ge=1, le=5)  # GARCH(p,q) order
    GARCH_Q: int = Field(default=1, ge=1, le=5)
    GARCH_DISTRIBUTION: str = Field(default="normal", pattern="^(normal|t|skewt)$")
    GARCH_REFIT_FREQUENCY_DAYS: int = Field(default=7, ge=1, le=30)  # Refit weekly
    EVT_THRESHOLD_PERCENTILE: float = Field(default=0.95, ge=0.90, le=0.99)
    EVT_MIN_TAIL_SAMPLES: int = Field(default=50, ge=30)
    MONTE_CARLO_SIMULATIONS: int = Field(default=10000, ge=1000, le=100000)
    STRESS_SCENARIO_PCT_DROP: list[Decimal] = Field(
        default=[Decimal("-0.05"), Decimal("-0.10"), Decimal("-0.15"), Decimal("-0.20")]
    )  # 5%, 10%, 15%, 20% portfolio drops
    CORRELATION_BREAK_THRESHOLD: float = Field(default=0.3, ge=0.1, le=0.5)
    VAR_ENGINE_TIMEOUT_SECONDS: float = Field(default=2.0, ge=0.5, le=10.0)
    VAR_ENGINE_FALLBACK_ON_TIMEOUT: bool = Field(default=True)
```

### 1.2 PositionLimitSettings

```python
class PositionLimitSettings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="POSITION_LIMIT_", env_file=".env", extra="ignore")

    MAX_NIFTY_LOTS: int = Field(default=5, ge=1, le=50)
    MAX_BANKNIFTY_LOTS: int = Field(default=3, ge=1, le=50)
    MAX_MCX_GOLD_LOTS: int = Field(default=2, ge=1, le=20)
    MAX_MCX_SILVER_LOTS: int = Field(default=2, ge=1, le=20)
    MAX_MCX_CRUDEOIL_LOTS: int = Field(default=2, ge=1, le=20)
    NIFTY_LOT_SIZE: int = Field(default=25, ge=1)
    BANKNIFTY_LOT_SIZE: int = Field(default=15, ge=1)
    MAX_GREEK_DELTA_EXPOSURE: Decimal = Field(default=Decimal("5000"), ge=0)
    MAX_GREEK_GAMMA_EXPOSURE: Decimal = Field(default=Decimal("500"), ge=0)
    MAX_GREEK_VEGA_EXPOSURE: Decimal = Field(default=Decimal("50000"), ge=0)
    MAX_GREEK_THETA_EXPOSURE: Decimal = Field(default=Decimal("3000"), ge=0)
    POSITION_LIMIT_SEBI_REFERENCE: str = "CIR/MRD/DP/09/2012 (exposure limits at individual client level)"
```

### 1.3 Validation Rules:
- `VAR_CONFIDENCE_LEVEL` must be >= 0.90 and <= 0.999 (McNeil Ch.2: common range 90%-99.9%)
- `CVAR_CONFIDENCE_LEVEL` must be >= `VAR_CONFIDENCE_LEVEL` (CVaR is always >= VaR in magnitude)
- `VAR_MAX_PORTFOLIO_CVAR` must be >= `VAR_MAX_PORTFOLIO_VAR` (CVaR >= VaR by definition)
- `GARCH_P` and `GARCH_Q` must be >= 1 (McNeil Ch.4: GARCH(1,1) is standard)
- `EVT_MIN_TAIL_SAMPLES` must be >= 30 (McNeil Ch.5: minimum for Hill estimator stability)
- Add a `@model_validator(mode="after")` to enforce cross-field validation

---

## Instruction 2 — Create `src/risk/var_engine.py` — VaR/CVaR/GARCH/EVT Quantitative Risk Engines

**Reference:** McNeil/Frey/Embrechts "Quantitative Risk Management" Ch.2 (VaR/CVaR), Ch.4 (GARCH), Ch.5 (EVT)

### 2.1 Enum VarMethod

```python
class VarMethod(Enum):
    HISTORICAL = "historical"
    PARAMETRIC = "parametric"
    MONTE_CARLO = "monte_carlo"
```

### 2.2 Dataclass VarResult

```python
@dataclass(frozen=True)
class VarResult:
    method: VarMethod
    confidence_level: float
    holding_period_days: int
    var_amount: Decimal  # Rs
    cvar_amount: Decimal  # Rs
    var_pct: float  # As % of portfolio
    cvar_pct: float  # As % of portfolio
    timestamp: datetime
    computation_time_ms: float
    data_points_used: int
```

### 2.3 Dataclass GarchResult

```python
@dataclass(frozen=True)
class GarchResult:
    model_type: str  # "GARCH", "EGARCH", "GJR"
    p: int
    q: int
    conditional_volatility: np.ndarray
    annualized_volatility: float
    persistence: float  # alpha + beta (stationarity condition: < 1)
    log_likelihood: float
    aic: float
    bic: float
    last_forecast: float  # 1-step ahead variance forecast
    forecast_annualized: float  # Annualized volatility forecast
    fitted_date: datetime
    residuals: np.ndarray
```

### 2.4 DataclassEvtResult

```python
@dataclass(frozen=True)
class EvtResult:
    threshold: float
    tail_index_xi: float  # Shape parameter (McNeil Ch.5: Hill estimator)
    tail_index_se: float  # Standard error
    n_exceedances: int
    var_evt: Decimal  # EVT-based VaR
    cvar_evt: Decimal  # EVT-based CVaR (Expected Shortfall)
    goodness_of_fit_pvalue: float  # Anderson-Darling test p-value
    timestamp: datetime
```

### 2.5 Dataclass StressTestResult

```python
@dataclass(frozen=True)
class StressTestResult:
    scenario_name: str
    pct_drop: Decimal
    portfolio_loss: Decimal
    projected_margin_utilization: Decimal
    would_trigger_kill_switch: bool
    would_breach_var_limit: bool
    greek_impact: dict  # {"delta": ..., "gamma": ..., "vega": ...}
```

### 2.6 Class HistoricalVarEngine

```python
class HistoricalVarEngine:
    """Historical Simulation VaR/CVaR per McNeil Ch.2.2.

    Uses actual historical P&L distribution without distributional assumptions.
    Fail-closed: if insufficient data (< lookback_days), reject order and log error.
    """

    def __init__(self, settings: QuantitativeRiskSettings):
        self._settings = settings

    def compute(
        self,
        pnl_series: pd.Series,
        portfolio_value: Decimal,
    ) -> VarResult:
        """
        Compute historical VaR and CVaR.

        Steps (McNeil Ch.2.2 Algorithm 2.1):
        1. Validate: len(pnl_series) >= VAR_LOOKBACK_DAYS, else raise InsufficientDataError
        2. Take last VAR_LOOKBACK_DAYS observations
        3. Sort P&L ascending
        4. VaR = -quantile(pnl, 1 - VAR_CONFIDENCE_LEVEL) * sqrt(VAR_HOLDING_PERIOD_DAYS)
        5. CVaR = -mean(pnl[pnl <= -VaR]) * sqrt(VAR_HOLDING_PERIOD_DAYS)
        6. Return VarResult

        Square-root-of-time scaling per McNeil Ch.2.3 (assumes i.i.d. returns).
        For GARCH-adjusted VaR, use GarchVarEngine instead.
        """
        ...

    def _validate_data(self, pnl_series: pd.Series) -> None:
        """Raise InsufficientDataError if len < VAR_LOOKBACK_DAYS."""
        ...

    def _time_scale(self, value: float, holding_period: int) -> float:
        """Square-root-of-time scaling: value * sqrt(holding_period)."""
        ...
```

### 2.7 Class ParametricVarEngine

```python
class ParametricVarEngine:
    """Parametric (Variance-Covariance) VaR/CVaR per McNeil Ch.2.3.

    Assumes P&L follows a normal distribution (or t-distribution if configured).
    McNeil Ch.2.3: VaR_alpha = mu - z_alpha * sigma * sqrt(T).
    For t-distribution: use scipy.stats.t.ppf instead of norm.ppf.
    """

    def __init__(self, settings: QuantitativeRiskSettings):
        self._settings = settings

    def compute(
        self,
        pnl_series: pd.Series,
        portfolio_value: Decimal,
    ) -> VarResult:
        """
        Compute parametric VaR and CVaR.

        Steps:
        1. Fit distribution parameters: mu = mean(pnl), sigma = std(pnl)
        2. If GARCH_DISTRIBUTION == "t": fit df via MLE (scipy.stats.t.fit)
        3. VaR = -(mu + z_alpha * sigma) * sqrt(T)
        4. CVaR = -E[P&L | P&L <= -VaR] using closed-form for normal/t
        5. Return VarResult
        """
        ...

    def _fit_distribution(self, pnl_series: pd.Series) -> dict:
        """Fit normal or t-distribution parameters."""
        ...

    def _cvar_closed_form(self, mu: float, sigma: float, alpha: float, df: float | None) -> float:
        """CVaR closed-form: for normal, E[X|X<=VaR] = mu - sigma * phi(z)/(1-alpha).
        For t-distribution: use scipy.stats.t.expect().
        McNeil Ch.2.3 Eq.2.16."""
        ...
```

### 2.8 Class MonteCarloVarEngine

```python
class MonteCarloVarEngine:
    """Monte Carlo Simulation VaR per McNeil Ch.2.4.

    Simulates portfolio P&L using fitted distribution or historical bootstrap.
    """

    def __init__(self, settings: QuantitativeRiskSettings):
        self._settings = settings

    def compute(
        self,
        pnl_series: pd.Series,
        portfolio_value: Decimal,
    ) -> VarResult:
        """
        Compute Monte Carlo VaR and CVaR.

        Steps:
        1. Fit distribution to pnl_series (normal or t)
        2. Generate MONTE_CARLO_SIMULATIONS random P&L scenarios
        3. Sort scenarios ascending
        4. VaR = -quantile(scenarios, 1 - alpha) * sqrt(T)
        5. CVaR = -mean(scenarios[scenarios <= -VaR]) * sqrt(T)
        6. Return VarResult

        Seed: use numpy.random.default_rng(42) for reproducibility in tests.
        Production: no seed (random).
        """
        ...
```

### 2.9 Class GarchVarEngine

```python
class GarchVarEngine:
    """GARCH-adjusted VaR per McNeil Ch.4.

    Fits GARCH(p,q) model to return series, forecasts 1-step ahead conditional
    volatility, then computes VaR using parametric approach with GARCH volatility.

    McNeil Ch.4: GARCH(1,1) is sufficient for most financial series.
    Persistence: alpha_1 + beta_1 < 1 (stationarity condition).
    Fail-closed: if GARCH fit fails, log error and return None (caller falls back to historical VaR).
    """

    def __init__(self, settings: QuantitativeRiskSettings):
        self._settings = settings
        self._model: arch.arch_model | None = None
        self._result: GarchResult | None = None
        self._last_fit_date: datetime | None = None

    def fit(self, return_series: pd.Series) -> GarchResult | None:
        """
        Fit GARCH model to return series.

        Steps (McNeil Ch.4.2-4.3):
        1. Validate: len(return_series) >= 2 * VAR_LOOKBACK_DAYS (need enough data)
        2. Create arch_model: arch_model(returns, vol=GARCH_MODEL_TYPE, p=GARCH_P, q=GARCH_Q, dist=GARCH_DISTRIBUTION)
        3. Fit model with max iterations=1000, show_warning=False
        4. Check persistence = alpha + beta < 1 (McNeil Ch.4.2: stationarity condition)
           If persistence >= 1: log WARNING "Non-stationary GARCH — using last valid fit"
        5. Compute 1-step ahead forecast
        6. Build GarchResult
        7. Cache model + result + last_fit_date
        8. Return GarchResult (or None if fit fails)
        """
        ...

    def needs_refit(self) -> bool:
        """Return True if GARCH_REFIT_FREQUENCY_DAYS have elapsed since last fit."""
        ...

    def compute_var(
        self,
        pnl_series: pd.Series,
        portfolio_value: Decimal,
        conditional_mean: float = 0.0,
    ) -> VarResult | None:
        """
        Compute GARCH-adjusted VaR.

        Steps:
        1. If not fitted or needs_refit: call fit()
        2. If fit failed: return None (caller falls back)
        3. VaR = -(conditional_mean + z_alpha * sigma_t) * sqrt(T)
           where sigma_t = last_forecast from GarchResult
        4. CVaR using parametric closed-form with GARCH volatility
        5. Return VarResult
        """
        ...

    def _diagnose_fit(self, result: arch.ARCHModelResult) -> dict:
        """Compute ACF/PACF of standardized residuals (McNeil Ch.4.5).
        Return {"lb_test_pvalue": ..., "acf_lag1": ...}.
        Ljung-Box test: if pvalue < 0.05, residuals have remaining ARCH effects (model insufficient).
        """
        ...
```

### 2.10 Class EvtEngine

```python
class EvtEngine:
    """Extreme Value Theory engine per McNeil Ch.5 (Peaks Over Threshold / GPD method).

    Models the tail of the P&L distribution using the Generalized Pareto Distribution (GPD).
    McNeil Ch.5.3: POT method with GPD fit to exceedances above threshold.

    Fail-closed: if insufficient exceedances (< EVT_MIN_TAIL_SAMPLES), return None.
    """

    def __init__(self, settings: QuantitativeRiskSettings):
        self._settings = settings

    def fit_gpd(self, pnl_series: pd.Series) -> EvtResult | None:
        """
        Fit GPD to tail losses using Peaks Over Threshold method.

        Steps (McNeil Ch.5.3 Algorithm 5.1):
        1. Take negative P&L (losses are positive)
        2. Set threshold at EVT_THRESHOLD_PERCENTILE quantile
        3. Compute exceedances = losses[losses > threshold] - threshold
        4. If len(exceedances) < EVT_MIN_TAIL_SAMPLES: return None with log WARNING
        5. Fit GPD(xi, beta) to exceedances using MLE (scipy.stats.genpareto.fit)
        6. Compute VaR_EVT = threshold + beta/xi * ((n/(Nu))^-xi - 1)  (McNeil Eq.5.14)
        7. Compute CVaR_EVT = VaR_EVT + (beta + xi * (VaR_EVT - threshold)) / (1 - xi)  (McNeil Eq.5.15)
           CRITICAL: Only valid when xi < 1 (finite ES). If xi >= 1: log ERROR "Infinite ES — EVT unreliable"
        8. Goodness-of-fit: Anderson-Darling test on exceedances vs fitted GPD
           If pvalue < 0.05: log WARNING "GPD fit rejected — EVT results unreliable"
        9. Return EvtResult
        """
        ...

    def _hill_estimator(self, losses: np.ndarray) -> tuple[float, float]:
        """Hill estimator for tail index xi (McNeil Ch.5.2).
        Returns (xi, standard_error).
        SE = xi / sqrt(k) where k = number of order statistics used.
        """
        ...
```

### 2.11 Class StressTestEngine

```python
class StressTestEngine:
    """Stress testing per McNeil Ch.9 (Scenario-based risk assessment).

    Applies predefined scenario shocks to current portfolio and measures impact.
    """

    def __init__(
        self,
        settings: QuantitativeRiskSettings,
        position_settings: PositionLimitSettings,
        risk_settings: RiskSettings,
    ):
        ...

    def run_scenarios(
        self,
        current_positions: list[dict],
        portfolio_value: Decimal,
        margin_utilization: Decimal,
    ) -> list[StressTestResult]:
        """
        Run all stress scenarios from STRESS_SCENARIO_PCT_DROP.

        For each scenario:
        1. Calculate portfolio_loss = portfolio_value * pct_drop
        2. Calculate projected_margin_utilization based on margin impact
        3. Check if would_trigger_kill_switch (margin >= MARGIN_UTILIZATION_KILL)
        4. Check if would_breach_var_limit (loss > VAR_MAX_PORTFOLIO_VAR)
        5. Estimate Greek impact (delta-adjusted loss for options positions)
        6. Return StressTestResult
        """
        ...

    def run_correlation_break(
        self,
        current_positions: list[dict],
        correlation_matrix: np.ndarray,
    ) -> StressTestResult:
        """
        Stress test: correlation breakdown (McNeil Ch.9.3).

        During crises, correlations spike toward 1. Recalculate portfolio VaR
        assuming all pairwise correlations = 1.0 and compare to normal VaR.
        If ratio > CORRELATION_BREAK_THRESHOLD: flag as correlation break risk.
        """
        ...
```

### 2.12 Class RiskEngineOrchestrator

```python
class RiskEngineOrchestrator:
    """Orchestrates all quantitative risk engines.

    Priority order for VaR computation (McNeil Ch.2-5):
    1. GARCH-adjusted VaR (best for conditional volatility clustering)
    2. Historical VaR (no distributional assumptions)
    3. Parametric VaR (fallback, assumes normality)

    EVT is used as supplementary tail risk check (not primary VaR method).
    Stress tests run on schedule (EOD) or on demand.

    Fail-closed on primary check (VaR limit), supplementary on EVT/GARCH.
    - If VaR > VAR_MAX_PORTFOLIO_VAR: REJECT order
    - If GARCH unavailable: log WARNING, continue with historical VaR (supplementary)
    - If EVT unavailable: log WARNING, continue without tail risk check (supplementary)
    - If VaR engine timeout (> VAR_ENGINE_TIMEOUT_SECONDS): if VAR_ENGINE_FALLBACK_ON_TIMEOUT, use cached result; else REJECT order
    """

    def __init__(
        self,
        quant_settings: QuantitativeRiskSettings,
        position_settings: PositionLimitSettings,
        risk_settings: RiskSettings,
        audit_logger: AuditLogger,
    ):
        self._historical = HistoricalVarEngine(quant_settings)
        self._parametric = ParametricVarEngine(quant_settings)
        self._monte_carlo = MonteCarloVarEngine(quant_settings)
        self._garch = GarchVarEngine(quant_settings)
        self._evt = EvtEngine(quant_settings)
        self._stress = StressTestEngine(quant_settings, position_settings, risk_settings)
        self._settings = quant_settings
        self._audit = audit_logger
        self._last_var_result: VarResult | None = None

    async def compute_var(self, pnl_series: pd.Series, portfolio_value: Decimal) -> VarResult:
        """
        Compute VaR with fallback chain.

        1. Try GARCH-adjusted VaR (if fitted and not stale)
        2. If GARCH fails/unavailable: fall back to Historical VaR
        3. If Historical fails (insufficient data): fall back to Parametric VaR
        4. Cache result as _last_var_result
        5. Log result via audit_logger
        6. Return VarResult
        """
        ...

    async def check_var_limit(self, pnl_series: pd.Series, portfolio_value: Decimal) -> bool:
        """
        Check if portfolio VaR is within limits.

        Returns True if VaR <= VAR_MAX_PORTFOLIO_VAR AND CVaR <= VAR_MAX_PORTFOLIO_CVAR.
        Returns False (reject) if either limit breached.
        Logs result via audit_logger with RISK_CHECK_PASSED or RISK_CHECK_FAILED event.
        """
        ...

    def run_stress_tests(
        self,
        current_positions: list[dict],
        portfolio_value: Decimal,
        margin_utilization: Decimal,
    ) -> list[StressTestResult]:
        """Run all stress test scenarios."""
        ...

    def get_risk_summary(self) -> dict:
        """Return current risk state: VaR, CVaR, GARCH volatility, EVT tail index, last stress test results."""
        ...
```

**All methods must be fully type-hinted. Use structlog for logging. Every VaR computation and limit check must be logged as an audit event.**

---

## Instruction 3 — Create `src/risk/self_trade_prevention.py`

### 3.1 Dataclass SelfTradeCheckResult

```python
@dataclass(frozen=True)
class SelfTradeCheckResult:
    is_self_trade: bool
    matching_order_id: str | None
    matching_side: str | None  # "BUY" or "SELL"
    reason: str
    sebi_reference: str = "CIR/MRD/DP/09/2012 (pre-trade risk controls)"
```

### 3.2 Class SelfTradePrevention

```python
class SelfTradePrevention:
    """Client-side self-trade prevention as defense-in-depth.

    Zerodha RMS is the primary enforcement. This is a supplementary client-side check.

    A self-trade occurs when:
    - New BUY order would match existing SELL order (same symbol, same or crossing price)
    - New SELL order would match existing BUY order (same symbol, same or crossing price)

    Per CIR/MRD/DP/09/2012: pre-trade risk controls must prevent erroneous orders.
    Self-trade prevention is an industry best practice (FIX Self-Trade Prevention, MiFID II).
    """

    def __init__(self, audit_logger: AuditLogger):
        self._audit = audit_logger
        self._open_orders: dict[str, dict] = {}  # order_id -> order_dict

    def check_order(self, new_order: dict) -> SelfTradeCheckResult:
        """
        Check if new_order would self-trade against existing open orders.

        Matching criteria:
        1. Same symbol (e.g., "NIFTY2470015000CE")
        2. Opposite side (BUY vs SELL)
        3. Price overlap: new_buy_price >= existing_sell_price OR new_sell_price <= existing_buy_price

        If match found: return SelfTradeCheckResult(is_self_trade=True)
        If no match: return SelfTradeCheckResult(is_self_trade=False)
        Log result via audit_logger.
        """
        ...

    def register_order(self, order_id: str, order: dict) -> None:
        """Add order to tracking after it's placed."""
        ...

    def remove_order(self, order_id: str) -> None:
        """Remove order from tracking after it's filled/cancelled."""
        ...

    def get_open_orders(self) -> dict[str, dict]:
        """Return current open orders dict."""
        ...

    def clear(self) -> None:
        """Clear all tracked orders (e.g., at session start)."""
        ...
```

---

## Instruction 4 — Create `src/risk/order_validator.py`

### 4.1 Dataclass OrderValidationResult

```python
@dataclass(frozen=True)
class OrderValidationResult:
    is_valid: bool
    errors: list[str]
    warnings: list[str]
    modified_order: dict | None  # None if invalid, sanitized order if valid with warnings
```

### 4.2 Class OrderValidator

```python
class OrderValidator:
    """Advanced order validation beyond Phase 0 basic checks.

    Validates order structure, modification limits, lot size compliance,
    and Zerodha-specific constraints.
    """

    def __init__(
        self,
        compliance_settings: ComplianceSettings,
        broker_settings: BrokerSettings,
        position_settings: PositionLimitSettings,
        audit_logger: AuditLogger,
    ):
        ...

    def validate_order_structure(self, order: dict) -> OrderValidationResult:
        """
        Validate order has all required fields with correct types.

        Required fields (per Kite API place_order):
        - exchange: str (one of ["NSE", "NFO", "MCX"])
        - tradingsymbol: str (Zerodha format, e.g., "NIFTY2470015000CE")
        - transaction_type: str ("BUY" or "SELL")
        - quantity: int (> 0, multiple of lot_size)
        - product: str ("MIS" for intraday, "NRML" for positional)
        - order_type: str ("MARKET", "LIMIT", "SL", "SL-M")
        - price: float | None (required for LIMIT/SL)
        - trigger_price: float | None (required for SL/SL-M)

        Returns OrderValidationResult with errors for missing/invalid fields.
        """
        ...

    def check_modification_limit(self, order_id: str, modification_count: int) -> OrderValidationResult:
        """
        Check if order has exceeded Zerodha's 25 modifications per order limit.

        Per Zerodha API docs: max 25 modifications per order.
        If modification_count >= 25: REJECT (suggest cancel + re-place instead).
        If modification_count >= 20: WARNING (approaching limit).
        Log via audit_logger.

        THIS REPLACES the erroneous "minimum_resting_time_check()" from Plan.txt.
        """
        ...

    def validate_lot_size(self, symbol: str, quantity: int) -> OrderValidationResult:
        """
        Validate quantity is a valid multiple of lot size.

        NIFTY: lot_size = 25 (PositionLimitSettings.NIFTY_LOT_SIZE)
        BANKNIFTY: lot_size = 15 (PositionLimitSettings.BANKNIFTY_LOTS)

        If quantity % lot_size != 0: REJECT with reason "Invalid lot multiple".
        """
        ...

    def validate_tradingsymbol_format(self, tradingsymbol: str) -> OrderValidationResult:
        """
        Validate Zerodha tradingsymbol format.

        Pattern for options: {SYMBOL}{YYM}{DD}{STRIKE}{CE|PE}
        Examples: "NIFTY2470015000CE", "BANKNIFTY247045000PE"

        Use regex to validate. If invalid: REJECT.
        """
        ...

    def validate_full(self, order: dict, modification_counts: dict[str, int] | None = None) -> OrderValidationResult:
        """
        Run all validations:
        1. Order structure
        2. Lot size
        3. Tradingsymbol format
        4. Modification limit (if order_id exists in modification_counts)

        Aggregate all errors and warnings.
        Return combined OrderValidationResult.
        """
        ...
```

---

## Instruction 5 — EXTEND `src/risk/manager.py` — Upgrade from 10 to 15 Pre-Trade Checks

**Action:** EXTEND the existing `RiskManager.pre_trade_check()` method to run 15 checks instead of 10. Keep all 10 existing checks intact. Add 5 new checks AFTER check 10.

### New Pre-Trade Check Table (checks 11-15):

| Check # | Name | Rule | SEBI/Standard Reference |
|---------|------|------|------------------------|
| 11 | VaR Limit | portfolio_var <= VAR_MAX_PORTFOLIO_VAR AND portfolio_cvar <= VAR_MAX_PORTFOLIO_CVAR | McNeil Ch.2-5 (VaR/CVaR risk limits) |
| 12 | Greek Exposure Limit | abs(portfolio_delta) <= MAX_GREEK_DELTA_EXPOSURE AND abs(portfolio_gamma) <= MAX_GREEK_GAMMA_EXPOSURE AND abs(portfolio_vega) <= MAX_GREEK_VEGA_EXPOSURE | McNeil Ch.9 (sensitivity-based limits) |
| 13 | Self-Trade Prevention | !SelfTradePrevention.check_order(order).is_self_trade | CIR/MRD/DP/09/2012 (pre-trade risk controls) |
| 14 | Order Modification Limit | modification_count < 25 per Zerodha API | Zerodha API documentation |
| 15 | OPS Registration Threshold | check_ops_threshold(current_ops, settings) — if REGISTERED, log WARNING that algo must be registered with exchange | SEBI/HO/MIRSD/MIRSD-PoD/P/CIR/2025/0000013 + NSE/INVG/67858 |

### 5.1 Updated RiskManager Constructor

Add new parameters to existing constructor:

```python
class RiskManager:
    def __init__(
        self,
        settings: ComplianceSettings,
        risk_settings: RiskSettings,
        kill_switch: KillSwitch,
        # NEW Phase 10 parameters:
        quant_settings: QuantitativeRiskSettings | None = None,
        position_settings: PositionLimitSettings | None = None,
        var_engine: RiskEngineOrchestrator | None = None,
        self_trade_prevention: SelfTradePrevention | None = None,
        order_validator: OrderValidator | None = None,
    ):
        # Keep all existing attributes
        # Add new attributes:
        self._quant_settings = quant_settings
        self._position_settings = position_settings
        self._var_engine = var_engine
        self._stp = self_trade_prevention
        self._order_validator = order_validator
        self._modification_counts: dict[str, int] = {}  # order_id -> count
```

### 5.2 New Check Methods

```python
    async def _check_var_limit(self, order: dict) -> PreTradeCheckDetail:
        """Check 11: VaR/CVaR limit.

        If var_engine is None: PASS with WARNING "VaR engine not configured — check skipped"
        If var_engine timeout ( > VAR_ENGINE_TIMEOUT_SECONDS):
          - If VAR_ENGINE_FALLBACK_ON_TIMEOUT and cached result available: use cached result
          - Else: FAIL (fail-closed)
        If VaR or CVaR exceeds limits: FAIL
        Else: PASS
        """
        ...

    async def _check_greek_exposure(self, order: dict) -> PreTradeCheckDetail:
        """Check 12: Greek exposure limits.

        Sum portfolio Greeks from current positions + projected new position.
        If any Greek exceeds limit: FAIL with specific Greek name and value.
        If position_settings is None: PASS with WARNING "Position limits not configured"
        If Greeks data unavailable: PASS with WARNING "Greeks data unavailable — check skipped"
          (Greeks are supplementary, not SEBI-mandated for retail)
        """
        ...

    async def _check_self_trade(self, order: dict) -> PreTradeCheckDetail:
        """Check 13: Self-trade prevention.

        If self_trade_prevention is None: PASS with WARNING "STP not configured"
        If check_order detects self-trade: FAIL with matching order details
        Else: PASS
        """
        ...

    async def _check_modification_limit(self, order: dict) -> PreTradeCheckDetail:
        """Check 14: Order modification limit.

        If order has order_id (modification), check modification_counts[order_id] < 25.
        If >= 25: FAIL — suggest cancel + re-place.
        If >= 20: PASS with WARNING "Approaching modification limit (20/25)".
        If new order (no order_id): PASS (not applicable).
        """
        ...

    async def _check_ops_registration(self, order: dict) -> PreTradeCheckDetail:
        """Check 15: OPS registration threshold.

        Per NSE/INVG/67858: if current OPS > 10, algo must be registered with exchange.
        This check does NOT reject the order — it logs a WARNING if registration is required.
        Always PASS — this is informational only (broker handles enforcement).
        """
        ...
```

### 5.3 Updated pre_trade_check Method

```python
    async def pre_trade_check(self, order: dict) -> PreTradeCheckResult:
        """Run ALL 15 pre-trade checks sequentially.

        Checks 1-10: from Phase 0 (unchanged)
        Checks 11-15: from Phase 10 (new)

        If ANY check returns FAIL: overall_result = FAIL
        If ALL checks return PASS (possibly with THROTTLED): overall_result = PASS/THROTTLED
        If kill_switch is KILL: overall_result = KILLED
        Every check result logged as audit event.
        """
        details: list[PreTradeCheckDetail] = []

        # Checks 1-10: existing Phase 0 checks (DO NOT modify)
        # ... (existing code unchanged)

        # Check 11: VaR limit
        details.append(await self._check_var_limit(order))

        # Check 12: Greek exposure limit
        details.append(await self._check_greek_exposure(order))

        # Check 13: Self-trade prevention
        details.append(await self._check_self_trade(order))

        # Check 14: Order modification limit
        details.append(await self._check_modification_limit(order))

        # Check 15: OPS registration threshold
        details.append(await self._check_ops_registration(order))

        # Aggregate results (existing logic unchanged)
        ...
```

### 5.4 Fail-Closed Principle

- **Checks 1-10 (from Phase 0):** If check cannot run (timeout/missing data), FAIL (existing behavior)
- **Check 11 (VaR Limit):** If VaR engine unavailable AND no cached result, FAIL (fail-closed). If VaR engine unavailable BUT cached result exists AND VAR_ENGINE_FALLBACK_ON_TIMEOUT=True, use cached.
- **Check 12 (Greek Exposure):** If Greeks data unavailable, PASS with WARNING (supplementary, not SEBI-mandated for retail)
- **Check 13 (Self-Trade Prevention):** If STP not configured, PASS with WARNING (Zerodha RMS is primary)
- **Check 14 (Modification Limit):** Always deterministic — modification count is tracked locally
- **Check 15 (OPS Threshold):** Always PASS — informational only

---

## Instruction 6 — EXTEND `src/risk/kill_switch.py` — Graduated Escalation Logic

**Action:** ADD graduated escalation logic to the existing KillSwitch class. Do NOT remove or modify existing methods.

### 6.1 New Method: `auto_escalate`

```python
    async def auto_escalate(self, trigger_source: str, reason: str) -> KillSwitchEvent:
        """Graduated escalation: INACTIVE -> THROTTLE -> PAUSE -> KILL.

        Per MiFID II Art.17: risk controls must include ability to throttle/pause/kill.
        Escalation only moves UP, never DOWN (must manually deactivate to reset).

        Rules:
        - Current INACTIVE -> escalate to THROTTLE
        - Current THROTTLE -> escalate to PAUSE
        - Current PAUSE -> escalate to KILL
        - Current KILL -> no-op (already at maximum)

        Log every escalation as audit event.
        Return KillSwitchEvent.
        """
        ...
```

### 6.2 New Method: `evaluate_auto_triggers`

```python
    async def evaluate_auto_triggers(
        self,
        current_pnl: Decimal,
        margin_utilization: Decimal,
        rejections_last_minute: int,
        daily_order_count: int,
        risk_settings: RiskSettings,
    ) -> KillSwitchEvent | None:
        """
        Evaluate all automatic trigger conditions and escalate if needed.

        Trigger conditions:
        1. Daily loss exceeded (current_pnl <= -DAILY_LOSS_LIMIT) -> KILL
        2. Margin utilization kill (>= MARGIN_UTILIZATION_KILL) -> KILL
        3. Margin utilization throttle (>= MARGIN_UTILIZATION_THRESHOLD) -> THROTTLE
        4. Rejection rate (>= ORDER_REJECTION_THRESHOLD) -> KILL
        5. Daily order count approaching limit (>= 80% MAX_ORDERS_PER_DAY) -> THROTTLE

        Priority: KILL conditions checked first (most urgent).
        If multiple triggers: take the highest severity.
        Log every evaluation (even if no trigger) as audit event.
        """
        ...
```

### 6.3 New Method: `get_escalation_history`

```python
    def get_escalation_history(self, limit: int = 20) -> list[KillSwitchEvent]:
        """Return the last N escalation events from activation_history.

        Filter to only show events where level changed (skip same-level activations).
        """
        ...
```

---

## Instruction 7 — EXTEND `src/risk/audit.py` — Dual-Write Audit Trail

**Action:** EXTEND AuditLogger to support dual-write (TimescaleDB + file). Do NOT remove existing functionality.

### 7.1 Add Dual-Write Support

```python
class AuditLogger:
    def __init__(self, settings: AuditSettings, db_pool: psycopg.AsyncConnectionPool | None = None):
        # Existing attributes (keep as-is)
        self._settings = settings
        # NEW: database connection pool
        self._db_pool = db_pool
        # NEW: file path for dual-write
        self._file_path = Path("logs/audit_trail.jsonl")
        self._file_path.parent.mkdir(parents=True, exist_ok=True)
        self._write_lock = asyncio.Lock()

    async def log_event(self, event_type: str, source: str, details: dict) -> AuditEvent:
        """
        Log audit event with dual-write: file + TimescaleDB.

        Steps:
        1. Create AuditEvent (existing logic — UUID, timestamp, checksum)
        2. Write to file (append, JSONL format) — ALWAYS (even if DB is down)
        3. Write to TimescaleDB (if db_pool available) — with retry (3 attempts, 100ms backoff)
        4. If DB write fails after retries: log WARNING "Audit DB write failed — file-only mode"
        5. Log to structlog (existing behavior)

        File format (JSONL):
        {"event_id": "...", "timestamp": "...", "event_type": "...", "source": "...", "details": {}, "checksum": "...", "ntp_offset_ms": ...}

        Per MiFID II Art.25(1): record keeping must be reliable.
        Per NIST AU-9: audit information must be protected from tampering.
        """
        ...
```

### 7.2 Add New Audit Event Types (Phase 10 specific)

```python
# Add to existing audit event type constants:
VAR_COMPUTED = "VAR_COMPUTED"
VAR_LIMIT_BREACHED = "VAR_LIMIT_BREACHED"
CVAR_LIMIT_BREACHED = "CVAR_LIMIT_BREACHED"
GARCH_FIT_COMPLETED = "GARCH_FIT_COMPLETED"
GARCH_FIT_FAILED = "GARCH_FIT_FAILED"
EVT_TAIL_RISK_DETECTED = "EVT_TAIL_RISK_DETECTED"
STRESS_TEST_COMPLETED = "STRESS_TEST_COMPLETED"
STRESS_TEST_BREACH = "STRESS_TEST_BREACH"
SELF_TRADE_DETECTED = "SELF_TRADE_DETECTED"
SELF_TRADE_PREVENTED = "SELF_TRADE_PREVENTED"
MODIFICATION_LIMIT_APPROACHED = "MODIFICATION_LIMIT_APPROACHED"
MODIFICATION_LIMIT_EXCEEDED = "MODIFICATION_LIMIT_EXCEEDED"
OPS_REGISTRATION_WARNING = "OPS_REGISTRATION_WARNING"
GREEK_LIMIT_BREACHED = "GREEK_LIMIT_BREACHED"
KILL_SWITCH_ESCALATED = "KILL_SWITCH_ESCALATED"
RISK_ENGINE_TIMEOUT = "RISK_ENGINE_TIMEOUT"
RISK_ENGINE_FALLBACK = "RISK_ENGINE_FALLBACK"
```

---

## Instruction 8 — EXTEND `deployment/init.sql` — Add New Hypertables

**Action:** ADD two new hypertables AFTER existing audit_events table. Do NOT modify existing SQL.

### 8.1 order_audit_trail Hypertable

```sql
-- Order-specific audit trail (detailed order lifecycle events)
CREATE TABLE IF NOT EXISTS order_audit_trail (
    event_id UUID PRIMARY KEY,
    timestamp TIMESTAMPTZ NOT NULL,
    order_id TEXT NOT NULL,
    strategy_name TEXT NOT NULL,
    action TEXT NOT NULL,  -- NEW, MODIFY, CANCEL, FILL, REJECT
    exchange TEXT NOT NULL,
    symbol TEXT NOT NULL,
    transaction_type TEXT NOT NULL,
    quantity INTEGER,
    price NUMERIC,
    trigger_price NUMERIC,
    rejection_reason TEXT,
    pnl_impact NUMERIC,
    risk_flags JSONB NOT NULL DEFAULT '{}',
    checksum VARCHAR(64) NOT NULL,
    ntp_offset_ms REAL,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

SELECT create_hypertable('order_audit_trail', 'timestamp', if_not_exists => TRUE);
SELECT add_retention_policy('order_audit_trail', INTERVAL '7 years', if_not_exists => TRUE);

CREATE INDEX IF NOT EXISTS idx_order_audit_order_id ON order_audit_trail (order_id);
CREATE INDEX IF NOT EXISTS idx_order_audit_strategy ON order_audit_trail (strategy_name);
CREATE INDEX IF NOT EXISTS idx_order_audit_symbol ON order_audit_trail (symbol);
CREATE INDEX IF NOT EXISTS idx_order_audit_timestamp ON order_audit_trail (timestamp DESC);
CREATE INDEX IF NOT EXISTS idx_order_audit_action ON order_audit_trail (action);

-- Prevent deletion (append-only, same as audit_events)
CREATE OR REPLACE FUNCTION prevent_order_audit_deletion()
RETURNS TRIGGER AS $$
BEGIN
    RAISE EXCEPTION 'Order audit events are append-only. Deletion not permitted.';
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_prevent_order_audit_deletion
BEFORE DELETE ON order_audit_trail
FOR EACH ROW EXECUTE FUNCTION prevent_order_audit_deletion();

CREATE OR REPLACE FUNCTION prevent_order_audit_update()
RETURNS TRIGGER AS $$
BEGIN
    RAISE EXCEPTION 'Order audit events are append-only. Updates not permitted.';
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_prevent_order_audit_update
BEFORE UPDATE ON order_audit_trail
FOR EACH ROW EXECUTE FUNCTION prevent_order_audit_update();
```

### 8.2 risk_audit_trail Hypertable

```sql
-- Risk-specific audit trail (VaR, Greeks, kill switch, stress test events)
CREATE TABLE IF NOT EXISTS risk_audit_trail (
    event_id UUID PRIMARY KEY,
    timestamp TIMESTAMPTZ NOT NULL,
    event_type TEXT NOT NULL,  -- VAR_COMPUTED, GARCH_FIT_COMPLETED, KILL_SWITCH_ESCALATED, etc.
    source TEXT NOT NULL,
    details JSONB NOT NULL DEFAULT '{}',
    checksum VARCHAR(64) NOT NULL,
    ntp_offset_ms REAL,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

SELECT create_hypertable('risk_audit_trail', 'timestamp', if_not_exists => TRUE);
SELECT add_retention_policy('risk_audit_trail', INTERVAL '7 years', if_not_exists => TRUE);

CREATE INDEX IF NOT EXISTS idx_risk_audit_event_type ON risk_audit_trail (event_type);
CREATE INDEX IF NOT EXISTS idx_risk_audit_source ON risk_audit_trail (source);
CREATE INDEX IF NOT EXISTS idx_risk_audit_timestamp ON risk_audit_trail (timestamp DESC);

-- Prevent deletion and updates (append-only)
CREATE OR REPLACE FUNCTION prevent_risk_audit_deletion()
RETURNS TRIGGER AS $$
BEGIN
    RAISE EXCEPTION 'Risk audit events are append-only. Deletion not permitted.';
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_prevent_risk_audit_deletion
BEFORE DELETE ON risk_audit_trail
FOR EACH ROW EXECUTE FUNCTION prevent_risk_audit_deletion();

CREATE OR REPLACE FUNCTION prevent_risk_audit_update()
RETURNS TRIGGER AS $$
BEGIN
    RAISE EXCEPTION 'Risk audit events are append-only. Updates not permitted.';
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_prevent_risk_audit_update
BEFORE UPDATE ON risk_audit_trail
FOR EACH ROW EXECUTE FUNCTION prevent_risk_audit_update();
```

---

## Instruction 9 — Create `src/risk/risk_monitor.py` — Real-Time Risk Monitoring

### 9.1 Dataclass RiskDashboardMetrics

```python
@dataclass(frozen=True)
class RiskDashboardMetrics:
    timestamp: datetime
    kill_switch_level: KillSwitchLevel
    daily_pnl: Decimal
    daily_order_count: int
    daily_rejection_count: int
    margin_utilization_pct: Decimal
    portfolio_var: Decimal | None
    portfolio_cvar: Decimal | None
    portfolio_delta: Decimal | None
    portfolio_gamma: Decimal | None
    portfolio_vega: Decimal | None
    garch_volatility_forecast: float | None
    current_ops: float
    compliance_level: ComplianceLevel
    active_positions_count: int
    last_stress_test_time: datetime | None
    risk_checks_total: int
    risk_checks_passed: int
    risk_checks_failed: int
```

### 9.2 Class RiskMonitor

```python
class RiskMonitor:
    """Real-time risk monitoring and dashboard metrics.

    Collects risk state from all risk components and produces
    a unified dashboard snapshot for Prometheus/Grafana.
    """

    def __init__(
        self,
        risk_manager: RiskManager,
        kill_switch: KillSwitch,
        var_engine: RiskEngineOrchestrator | None = None,
        audit_logger: AuditLogger | None = None,
    ):
        ...

    def get_dashboard_metrics(self) -> RiskDashboardMetrics:
        """
        Collect current risk state from all components.

        Sources:
        - Kill switch: current level
        - Risk manager: daily counts, P&L, check statistics
        - VaR engine: latest VaR/CVaR, GARCH forecast
        - Audit logger: recent check results
        Return RiskDashboardMetrics.
        """
        ...

    def get_prometheus_metrics(self) -> str:
        """
        Format metrics for Prometheus scraping.

        Metrics (all prefixed with trading_bot_):
        - trading_bot_kill_switch_level: gauge (0=INACTIVE, 1=THROTTLE, 2=PAUSE, 3=KILL)
        - trading_bot_daily_pnl: gauge
        - trading_bot_daily_orders: counter
        - trading_bot_daily_rejections: counter
        - trading_bot_margin_utilization: gauge (0-1)
        - trading_bot_portfolio_var: gauge
        - trading_bot_portfolio_cvar: gauge
        - trading_bot_portfolio_delta: gauge
        - trading_bot_portfolio_gamma: gauge
        - trading_bot_portfolio_vega: gauge
        - trading_bot_garch_volatility: gauge
        - trading_bot_ops: gauge
        - trading_bot_risk_checks_total: counter
        - trading_bot_risk_checks_failed: counter

        Format: https://prometheus.io/docs/instrumenting/exposition_formats/
        """
        ...

    def check_health(self) -> dict:
        """
        Health check for risk subsystems.

        Checks:
        1. Kill switch: should be INACTIVE (or THROTTLE) for healthy
        2. VaR engine: should have recent computation (< 1 hour old)
        3. Audit logger: file should be writable
        4. GARCH model: should be fitted and not stale (< GARCH_REFIT_FREQUENCY_DAYS old)
        5. Margin utilization: should be < MARGIN_UTILIZATION_THRESHOLD

        Return {"status": "healthy"|"degraded"|"critical", "details": {...}}
        """
        ...
```

---

## Instruction 10 — Create `src/risk/risk_api.py` — REST API Endpoints

### 10.1 FastAPI Application

```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel

app = FastAPI(
    title="Indian Trading Bot — Risk Management API",
    version="10.0.0",
    description="REST API for risk controls, kill switch, and monitoring",
)
```

### 10.2 Pydantic Request/Response Models

```python
class KillSwitchRequest(BaseModel):
    level: str  # "THROTTLE", "PAUSE", "KILL"
    source: str = "rest_api"
    reason: str

class KillSwitchResponse(BaseModel):
    previous_level: str
    new_level: str
    timestamp: str

class RiskCheckRequest(BaseModel):
    order: dict

class RiskCheckResponse(BaseModel):
    overall_result: str  # "PASS", "FAIL", "THROTTLED", "KILLED"
    details: list[dict]
    timestamp: str

class VarRequest(BaseModel):
    portfolio_value: float
    lookback_days: int | None = None

class VarResponse(BaseModel):
    method: str
    var_amount: float
    cvar_amount: float
    var_pct: float
    cvar_pct: float
    timestamp: str

class StressTestResponse(BaseModel):
    scenarios: list[dict]
    timestamp: str

class DashboardResponse(BaseModel):
    metrics: dict
    timestamp: str

class HealthResponse(BaseModel):
    status: str
    details: dict
```

### 10.3 API Endpoints

```python
@app.post("/kill-switch", response_model=KillSwitchResponse)
async def activate_kill_switch(request: KillSwitchRequest):
    """POST /kill-switch — Activate kill switch.

    Per NIST RS.RP-1, MiFID II Art.17, ISO A.8.26.
    Authentication: API key header (to be implemented in Phase 11).
    """
    ...

@app.delete("/kill-switch", response_model=KillSwitchResponse)
async def deactivate_kill_switch(reason: str = "Manual REST API deactivation"):
    """DELETE /kill-switch — Deactivate kill switch (manual re-enable).

    Only allowed if REQUIRE_MANUAL_RE_ENABLE is True and request is explicit.
    """
    ...

@app.post("/risk-check", response_model=RiskCheckResponse)
async def run_risk_check(request: RiskCheckRequest):
    """POST /risk-check — Run all 15 pre-trade checks on an order.

    Returns detailed results for each check.
    """
    ...

@app.get("/var", response_model=VarResponse)
async def get_var(portfolio_value: float):
    """GET /var — Get latest VaR/CVaR computation."""
    ...

@app.post("/var/compute", response_model=VarResponse)
async def compute_var(request: VarRequest):
    """POST /var/compute — Force VaR recomputation."""
    ...

@app.get("/stress-test", response_model=StressTestResponse)
async def get_stress_test_results():
    """GET /stress-test — Get latest stress test results."""
    ...

@app.post("/stress-test/run", response_model=StressTestResponse)
async def run_stress_test():
    """POST /stress-test/run — Run stress tests now."""
    ...

@app.get("/dashboard", response_model=DashboardResponse)
async def get_dashboard():
    """GET /dashboard — Get current risk dashboard metrics."""
    ...

@app.get("/health", response_model=HealthResponse)
async def get_health():
    """GET /health — Risk subsystem health check."""
    ...

@app.get("/garch/status")
async def get_garch_status():
    """GET /garch/status — GARCH model fit status and diagnostics."""
    ...
```

---

## Instruction 11 — Test Suite: Unit Tests

### 11.1 `tests/risk/test_var_engine.py`

```python
# Tests for VaR/CVaR engines

# --- HistoricalVarEngine ---
test_historical_var_with_normal_data:
    """Generate 252 days of normal P&L, compute VaR at 95%.
    Verify: VaR > 0, CVaR > VaR (CVaR >= VaR by definition per McNeil Ch.2)."""

test_historical_var_insufficient_data:
    """Pass only 10 data points when 252 required.
    Verify: raises InsufficientDataError."""

test_historical_var_with_extreme_loss:
    """Add extreme loss to P&L series.
    Verify: CVaR increases significantly (tail-sensitive per McNeil Ch.2.2)."""

test_historical_var_time_scaling:
    """Compute VaR with holding_period=1 and holding_period=4.
    Verify: var_4day ≈ var_1day * 2 (square-root-of-time scaling)."""

# --- ParametricVarEngine ---
test_parametric_var_normal:
    """Compute parametric VaR assuming normal distribution.
    Verify: VaR ≈ -1.645 * sigma (95% confidence)."""

test_parametric_var_t_distribution:
    """Compute parametric VaR with t-distribution (df=5).
    Verify: t-VaR > normal-VaR (heavier tails = higher VaR per McNeil Ch.2.3)."""

# --- MonteCarloVarEngine ---
test_monte_carlo_var_reproducibility:
    """Compute Monte Carlo VaR twice with seed=42.
    Verify: both results are identical."""

test_monte_carlo_var_convergence:
    """Compute MC VaR with 1000 and 100000 simulations.
    Verify: 100K result is within 5% of 1K result (convergence)."""

# --- GarchVarEngine ---
test_garch_fit_garch11:
    """Fit GARCH(1,1) to simulated return series with known parameters.
    Verify: persistence < 1, annualized volatility > 0."""

test_garch_non_stationary_warning:
    """Fit GARCH to series where alpha+beta >= 1.
    Verify: log WARNING about non-stationarity."""

test_garch_needs_refit:
    """After fitting, advance clock by GARCH_REFIT_FREQUENCY_DAYS + 1.
    Verify: needs_refit() returns True."""

test_garch_var_vs_historical:
    """Compare GARCH-adjusted VaR to historical VaR.
    Verify: GARCH VaR reflects current conditional volatility (may be higher or lower than historical)."""

# --- EvtEngine ---
test_evt_gpd_fit:
    """Generate losses from known GPD(xi=0.3, beta=1.0).
    Verify: fitted xi and beta are within 20% of true values."""

test_evt_insufficient_exceedances:
    """Pass series with very few exceedances (< EVT_MIN_TAIL_SAMPLES).
    Verify: fit_gpd returns None with WARNING log."""

test_evt_infinite_es_warning:
    """Fit GPD with xi >= 1.
    Verify: log ERROR about infinite ES."""

test_evt_var_cvar_relationship:
    """Compute EVT VaR and CVaR.
    Verify: CVaR > VaR (always true per McNeil Ch.5)."""

# --- StressTestEngine ---
test_stress_test_5pct_drop:
    """Run 5% drop stress scenario on Rs 10L portfolio.
    Verify: portfolio_loss = 50000, greek_impact populated."""

test_stress_test_kill_switch_trigger:
    """Run stress test with 20% drop when margin utilization is 85%.
    Verify: would_trigger_kill_switch = True for >= MARGIN_UTILIZATION_KILL scenario."""

test_stress_test_correlation_break:
    """Run correlation break test.
    Verify: result includes correlation break assessment."""

# --- RiskEngineOrchestrator ---
test_orchestrator_fallback_to_historical:
    """When GARCH not fitted: verify falls back to historical VaR."""

test_orchestrator_fallback_to_parametric:
    """When historical data insufficient: verify falls back to parametric VaR."""

test_orchestrator_var_limit_pass:
    """Portfolio VaR below limit: verify check_var_limit returns True."""

test_orchestrator_var_limit_fail:
    """Portfolio VaR above limit: verify check_var_limit returns False, audit event logged."""

test_orchestrator_timeout_fallback:
    """Simulate VaR engine timeout.
    Verify: if VAR_ENGINE_FALLBACK_ON_TIMEOUT, uses cached result; else fails."""
```

### 11.2 `tests/risk/test_self_trade_prevention.py`

```python
test_stp_no_self_trade:
    """BUY NIFTY with no existing SELL orders: is_self_trade = False."""

test_stp_matching_buy_sell:
    """BUY NIFTY2470015000CE at 100 when existing SELL at 100: is_self_trade = True."""

test_stp_price_overlap:
    """BUY at 105 when existing SELL at 100: is_self_trade = True (price overlap)."""

test_stp_no_price_overlap:
    """BUY at 95 when existing SELL at 100: is_self_trade = False (no overlap)."""

test_stp_different_symbols:
    """BUY NIFTY with existing SELL BANKNIFTY: is_self_trade = False (different symbol)."""

test_stp_same_side:
    """BUY with existing BUY: is_self_trade = False (same side)."""

test_stp_register_and_remove:
    """Register order, verify in get_open_orders, remove order, verify removed."""

test_stp_clear:
    """Add 5 orders, clear, verify get_open_orders is empty."""
```

### 11.3 `tests/risk/test_order_validator.py`

```python
test_validate_order_structure_valid:
    """Valid order dict with all required fields: is_valid = True."""

test_validate_order_structure_missing_exchange:
    """Order without exchange: is_valid = False, error contains 'exchange'."""

test_validate_order_structure_invalid_quantity:
    """quantity = 0: is_valid = False, error contains 'quantity'."""

test_validate_order_structure_limit_without_price:
    """LIMIT order without price: is_valid = False, error contains 'price'."""

test_check_modification_limit_below:
    """modification_count = 10: is_valid = True."""

test_check_modification_limit_approaching:
    """modification_count = 21: is_valid = True, warnings contains 'approaching'."""

test_check_modification_limit_exceeded:
    """modification_count = 25: is_valid = False, error contains '25'."""

test_validate_lot_size_valid_nifty:
    """quantity = 50 (2 lots of 25): is_valid = True."""

test_validate_lot_size_invalid_nifty:
    """quantity = 30 (not multiple of 25): is_valid = False, error contains 'lot'."""

test_validate_tradingsymbol_valid:
    """'NIFTY2470015000CE': is_valid = True."""

test_validate_tradingsymbol_invalid:
    """'NIFTY_15000': is_valid = False."""

test_validate_full_all_pass:
    """Valid order, no modifications: is_valid = True, no errors, no warnings."""

test_validate_full_multiple_errors:
    """Invalid order with multiple issues: is_valid = False, multiple errors."""
```

### 11.4 `tests/risk/test_risk_monitor.py`

```python
test_dashboard_metrics_initial:
    """New RiskMonitor: daily_pnl = 0, kill_switch_level = INACTIVE."""

test_dashboard_metrics_with_var:
    """After VaR computation: portfolio_var and portfolio_cvar populated."""

test_prometheus_metrics_format:
    """Verify Prometheus text format: metric_name value, no trailing commas."""

test_health_check_healthy:
    """All subsystems normal: status = 'healthy'."""

test_health_check_degraded:
    """GARCH model stale, VaR > 1 hour old: status = 'degraded'."""

test_health_check_critical:
    """Kill switch at KILL level: status = 'critical'."""
```

### 11.5 `tests/risk/test_risk_api.py`

```python
# Use FastAPI TestClient (httpx.AsyncClient)

test_post_kill_switch:
    """POST /kill-switch with level=KILL: returns 200, kill switch activated."""

test_delete_kill_switch:
    """DELETE /kill-switch: returns 200, kill switch deactivated."""

test_post_risk_check_pass:
    """POST /risk-check with valid order: overall_result = PASS."""

test_post_risk_check_fail:
    """POST /risk-check with invalid symbol: overall_result = FAIL."""

test_get_var:
    """GET /var: returns latest VaR result."""

test_get_dashboard:
    """GET /dashboard: returns metrics dict."""

test_get_health:
    """GET /health: returns status dict."""
```

---

## Instruction 12 — Test Suite: Property-Based Tests (Hypothesis)

### 12.1 `tests/property/test_var_properties.py`

```python
from hypothesis import given, settings, assume
from hypothesis import strategies as st
import numpy as np

# Strategy: generate P&L series
pnl_series_strategy = st.lists(
    st.floats(min_value=-100000, max_value=100000, allow_nan=False, allow_infinity=False),
    min_size=30,
    max_size=500,
).map(lambda x: pd.Series(x))

@given(pnl_series_strategy)
@settings(max_examples=100)
def test_var_always_positive(pnl_series):
    """Property: VaR is always >= 0 (it's a loss threshold, not a P&L value).
    Per McNeil Ch.2: VaR_alpha(X) = -q_alpha(X) where q_alpha is the alpha-quantile of the loss distribution."""
    assume(len(pnl_series) >= 252)
    engine = HistoricalVarEngine(QuantitativeRiskSettings())
    result = engine.compute(pnl_series, Decimal("1000000"))
    assert result.var_amount >= 0

@given(pnl_series_strategy)
@settings(max_examples=100)
def test_cvar_always_gte_var(pnl_series):
    """Property: CVaR >= VaR (always, by definition).
    Per McNeil Ch.2: ES_alpha >= VaR_alpha for any distribution."""
    assume(len(pnl_series) >= 252)
    engine = HistoricalVarEngine(QuantitativeRiskSettings())
    result = engine.compute(pnl_series, Decimal("1000000"))
    assert result.cvar_amount >= result.var_amount

@given(
    st.floats(min_value=0.90, max_value=0.999),
    st.floats(min_value=0.90, max_value=0.999),
)
def test_higher_confidence_higher_var(alpha1, alpha2):
    """Property: Higher confidence level produces higher VaR.
    Per McNeil Ch.2: VaR is monotonically increasing in confidence level."""
    assume(alpha2 > alpha1)
    settings1 = QuantitativeRiskSettings(VAR_CONFIDENCE_LEVEL=alpha1)
    settings2 = QuantitativeRiskSettings(VAR_CONFIDENCE_LEVEL=alpha2)
    pnl = pd.Series(np.random.normal(0, 1000, 252))
    result1 = HistoricalVarEngine(settings1).compute(pnl, Decimal("1000000"))
    result2 = HistoricalVarEngine(settings2).compute(pnl, Decimal("1000000"))
    assert result2.var_amount >= result1.var_amount

@given(st.integers(min_value=1, max_value=10))
def test_time_scaling_increases_var(holding_period):
    """Property: Square-root-of-time scaling increases VaR with holding period.
    Per McNeil Ch.2.3: VaR_T = VaR_1 * sqrt(T)."""
    pnl = pd.Series(np.random.normal(0, 1000, 252))
    settings_1d = QuantitativeRiskSettings(VAR_HOLDING_PERIOD_DAYS=1)
    settings_Td = QuantitativeRiskSettings(VAR_HOLDING_PERIOD_DAYS=holding_period)
    result_1d = HistoricalVarEngine(settings_1d).compute(pnl, Decimal("1000000"))
    result_Td = HistoricalVarEngine(settings_Td).compute(pnl, Decimal("1000000"))
    assert result_Td.var_amount >= result_1d.var_amount * 0.9  # Allow 10% tolerance
```

### 12.2 `tests/property/test_risk_invariants.py`

```python
@given(
    st.dictionaries(
        st.text(min_size=1, max_size=20, alphabet=string.ascii_uppercase),
        st.floats(min_value=-1000000, max_value=1000000, allow_nan=False),
    )
)
@settings(max_examples=200)
def test_pre_trade_check_never_passes_invalid_symbol(order_overrides):
    """Property: Any order with symbol not in ALLOWED_SEGMENTS must FAIL pre-trade check.
    Per CIR/MRD/DP/09/2012: pre-trade risk controls must validate instrument eligibility."""
    ...

@given(
    st.floats(min_value=0, max_value=1),  # margin_utilization
    st.integers(min_value=0, max_value=20),  # rejections_last_minute
    st.floats(min_value=-200000, max_value=200000),  # daily_pnl
)
def test_kill_switch_escalation_is_monotonic(margin_util, rejections, pnl):
    """Property: Kill switch level only increases via auto_escalate, never decreases.
    Per MiFID II Art.17: risk controls must be non-reversible without manual intervention."""
    ...

@given(st.lists(st.floats(min_value=-1000, max_value=1000, allow_nan=False), min_size=50))
def test_garch_persistence_below_1(returns):
    """Property: Fitted GARCH(1,1) persistence (alpha+beta) must be < 1 for stationarity.
    Per McNeil Ch.4.2: non-stationary GARCH is invalid."""
    ...

@given(
    st.integers(min_value=1, max_value=1000),
    st.integers(min_value=1, max_value=50),
)
def test_lot_size_validation(quantity, lot_size):
    """Property: quantity must be divisible by lot_size for valid order.
    If quantity % lot_size != 0: validation must fail."""
    ...
```

### 12.3 `tests/property/test_self_trade_properties.py`

```python
@given(
    st.sampled_from(["NIFTY2470015000CE", "BANKNIFTY247045000PE", "GOLD2470065000CE"]),
    st.floats(min_value=1, max_value=100000, allow_nan=False),
    st.floats(min_value=1, max_value=100000, allow_nan=False),
)
def test_self_trade_symmetry(symbol, buy_price, sell_price):
    """Property: If BUY at X matches SELL at Y, then SELL at Y matches BUY at X.
    Self-trade check is symmetric."""
    ...

@given(st.lists(st.floats(min_value=1, max_value=10000, allow_nan=False), min_size=0, max_size=50))
def test_self_trade_no_false_positives_same_side(prices):
    """Property: Orders on the same side (all BUY or all SELL) never trigger self-trade."""
    ...
```

---

## Instruction 13 — Test Suite: Replay/Chaos Tests

### 13.1 `tests/replay/test_risk_under_chaos.py`

```python
test_risk_check_with_missing_market_data:
    """Simulate: LTP unavailable (None).
    Verify: price protection check FAILS (fail-closed) with appropriate reason."""

test_risk_check_with_stale_var:
    """Simulate: VaR engine returns result from >1 hour ago.
    Verify: risk check passes but logs WARNING about stale VaR."""

test_risk_check_with_var_timeout:
    """Simulate: VaR computation takes > VAR_ENGINE_TIMEOUT_SECONDS.
    Verify: if VAR_ENGINE_FALLBACK_ON_TIMEOUT, uses cached; else FAILS."""

test_risk_check_with_zero_portfolio:
    """Simulate: portfolio_value = 0.
    Verify: VaR = 0, check passes (no risk if no exposure)."""

test_kill_switch_during_order_flow:
    """Simulate: kill switch activated WHILE pre-trade checks are running.
    Verify: order is REJECTED even if checks started before kill switch activation.
    Race condition test: use asyncio.Lock to ensure consistency."""

test_garch_fit_with_nan_returns:
    """Simulate: return series contains NaN values.
    Verify: GARCH fit handles NaN gracefully (drop or raise, don't crash)."""

test_garch_fit_with_constant_returns:
    """Simulate: all returns are identical (zero volatility).
    Verify: GARCH fit returns None or raises, doesn't produce division by zero."""

test_audit_dual_write_db_down:
    """Simulate: TimescaleDB connection fails.
    Verify: audit events still written to file (graceful degradation)."""

test_self_trade_with_concurrent_orders:
    """Simulate: 100 concurrent orders submitted simultaneously.
    Verify: self-trade check is thread-safe (asyncio.Lock), no false negatives."""

test_margin_check_with_broker_api_timeout:
    """Simulate: broker margin API takes >5 seconds.
    Verify: margin check uses last known margin with WARNING (fail-open with timeout for margin only, per CIR/MRD/DP/09/2012 margin checks are broker-side)."""

test_risk_check_with_negative_pnl_series:
    """Simulate: all P&L values are negative (sustained losses).
    Verify: VaR/CVaR computed correctly (high values), limit check may FAIL."""

test_stress_test_with_empty_positions:
    """Simulate: no open positions.
    Verify: stress test returns 0 loss for all scenarios."""

test_order_validator_with_max_field_lengths:
    """Simulate: tradingsymbol at max Zerodha length, reason at max length.
    Verify: validation passes without truncation issues."""
```

---

## Instruction 14 — Test Suite: Integration Tests

### 14.1 `tests/integration/test_risk_full_pipeline.py`

```python
test_full_risk_pipeline_pass:
    """
    End-to-end: valid order goes through all 15 checks -> PASS -> logged to audit trail.

    Setup:
    1. Create RiskManager with all Phase 10 engines
    2. Load P&L series (252 days)
    3. Fit GARCH model
    4. Compute initial VaR
    5. Submit valid order

    Verify:
    - PreTradeCheckResult.overall_result == PASS
    - All 15 details have result PASS
    - Audit event logged to file
    - Dashboard metrics updated
    """

test_full_risk_pipeline_var_breach:
    """
    End-to-end: portfolio VaR exceeds limit -> order REJECTED.

    Setup:
    1. Create RiskManager with VAR_MAX_PORTFOLIO_VAR = Rs 10K
    2. Load P&L series with high volatility
    3. Submit order

    Verify:
    - PreTradeCheckResult.overall_result == FAIL
    - Check 11 (VaR limit) has result FAIL
    - Audit event: VAR_LIMIT_BREACHED
    """

test_full_risk_pipeline_kill_switch_escalation:
    """
    End-to-end: daily loss triggers auto-escalation.

    Setup:
    1. Create KillSwitch + RiskManager
    2. Set current_pnl to -60000 (exceeds DAILY_LOSS_LIMIT of 50000)
    3. Call evaluate_auto_triggers

    Verify:
    - Kill switch escalated to KILL
    - Next order rejected (check 10)
    - Audit events: KILL_SWITCH_ESCALATED, RISK_CHECK_FAILED
    - Dashboard shows kill_switch_level = KILL
    """

test_full_risk_pipeline_self_trade_blocked:
    """
    End-to-end: self-trade detected -> order REJECTED.

    Setup:
    1. Place BUY order for NIFTY2470015000CE at 100
    2. Register order in STP
    3. Try to place SELL order for same symbol at 100

    Verify:
    - PreTradeCheckResult.overall_result == FAIL
    - Check 13 (Self-Trade Prevention) has result FAIL
    - Audit event: SELF_TRADE_PREVENTED
    """

test_full_risk_pipeline_garch_to_var_to_check:
    """
    End-to-end: GARCH fit -> VaR computation -> risk check.

    Setup:
    1. Load 2 years of daily returns
    2. Fit GARCH(1,1)
    3. Compute VaR using GARCH volatility
    4. Run pre-trade check with VaR limit

    Verify:
    - GARCH fit succeeds (persistence < 1)
    - VaR computed with GARCH forecast
    - If VaR within limits: check passes
    - Audit events: GARCH_FIT_COMPLETED, VAR_COMPUTED, RISK_CHECK_PASSED
    """

test_dual_write_audit_recovery:
    """
    End-to-end: DB goes down mid-session -> file-only mode -> DB comes back -> verify gap.

    Setup:
    1. Start with both file + DB writes
    2. Simulate DB failure (mock db_pool)
    3. Generate 10 audit events (should go to file only)
    4. Simulate DB recovery
    5. Generate 10 more events (should go to both)

    Verify:
    - All 20 events in file
    - First 10 and last 10 in DB
    - Gap of 10 events in DB (with WARNING logs)
    """
```

---

## Instruction 15 — Test Suite: Performance Benchmarks

### 15.1 `tests/bench/test_var_benchmark.py`

```python
def test_historical_var_benchmark_252_days(benchmark):
    """Benchmark: Historical VaR computation with 252 data points.
    Target: < 10ms per McNeil Ch.2 (simple quantile computation)."""
    ...

def test_parametric_var_benchmark_252_days(benchmark):
    """Benchmark: Parametric VaR computation with 252 data points.
    Target: < 5ms (only mean/std computation)."""
    ...

def test_monte_carlo_var_benchmark_10k(benchmark):
    """Benchmark: Monte Carlo VaR with 10,000 simulations.
    Target: < 100ms."""
    ...

def test_garch_fit_benchmark_252_days(benchmark):
    """Benchmark: GARCH(1,1) fit with 252 data points.
    Target: < 500ms (arch package is Python-native, so slower)."""
    ...

def test_evt_gpd_fit_benchmark(benchmark):
    """Benchmark: EVT GPD fit with 500 data points.
    Target: < 50ms."""
    ...

def test_pre_trade_check_15_checks_benchmark(benchmark):
    """Benchmark: Full 15-check pre-trade pipeline.
    Target: < 20ms (without VaR recomputation, using cached)."""
    ...

def test_self_trade_check_benchmark_50_orders(benchmark):
    """Benchmark: Self-trade check with 50 open orders.
    Target: < 1ms (O(n) scan, 50 orders is typical)."""
    ...

def test_stress_test_benchmark(benchmark):
    """Benchmark: Run all 4 stress scenarios.
    Target: < 5ms."""
    ...
```

---

## Instruction 16 — Test Suite: Load Tests (Locust)

### 16.1 `tests/load/locustfile_risk_api.py`

```python
from locust import HttpUser, task, between

class RiskAPIUser(HttpUser):
    wait_time = between(0.1, 1.0)
    host = "http://localhost:8000"

    @task(5)
    def get_dashboard(self):
        """Most frequent: dashboard polling (every 1s from Grafana)."""
        self.client.get("/dashboard")

    @task(3)
    def get_health(self):
        """Health check polling (every 10s from Prometheus)."""
        self.client.get("/health")

    @task(2)
    def get_var(self):
        """VaR query (on demand)."""
        self.client.get("/var")

    @task(1)
    def run_risk_check(self):
        """Pre-trade risk check (per order)."""
        self.client.post("/risk-check", json={
            "order": {
                "exchange": "NFO",
                "tradingsymbol": "NIFTY2470015000CE",
                "transaction_type": "BUY",
                "quantity": 25,
                "product": "MIS",
                "order_type": "LIMIT",
                "price": 150.0,
            }
        })

    @task(0.1)
    def activate_kill_switch(self):
        """Kill switch activation (rare, emergency only)."""
        self.client.post("/kill-switch", json={
            "level": "KILL",
            "source": "load_test",
            "reason": "Load test emergency"
        })
```

### 16.2 Load Test Scenarios

- **Normal load:** 10 users, 1 req/s each = 10 RPS — sustained for 5 minutes
- **Peak load:** 50 users, 5 req/s each = 250 RPS — sustained for 2 minutes
- **Burst:** 100 users, spike to 10 req/s for 30 seconds
- **Stress:** Ramp to 200 users, sustained for 5 minutes — verify no crashes, no memory leaks

**Pass criteria:**
- p99 latency < 100ms for /dashboard, /health
- p99 latency < 200ms for /risk-check
- p99 latency < 500ms for /var/compute
- 0% 5xx errors
- Memory growth < 100MB over 5-minute run

---

## Instruction 17 — Update `tests/conftest.py` — Add Phase 10 Fixtures

**Action:** EXTEND `tests/conftest.py` — add new fixtures AFTER existing ones. Do NOT modify existing fixtures.

```python
@pytest.fixture
def quant_risk_settings() -> QuantitativeRiskSettings:
    return QuantitativeRiskSettings(
        VAR_CONFIDENCE_LEVEL=0.95,
        VAR_LOOKBACK_DAYS=252,
        VAR_HOLDING_PERIOD_DAYS=1,
        VAR_METHOD="historical",
        CVAR_CONFIDENCE_LEVEL=0.975,
        VAR_MAX_PORTFOLIO_VAR=Decimal("100000"),
        VAR_MAX_PORTFOLIO_CVAR=Decimal("150000"),
        GARCH_MODEL_TYPE="GARCH",
        GARCH_P=1,
        GARCH_Q=1,
        GARCH_DISTRIBUTION="normal",
        GARCH_REFIT_FREQUENCY_DAYS=7,
        EVT_THRESHOLD_PERCENTILE=0.95,
        EVT_MIN_TAIL_SAMPLES=30,
        MONTE_CARLO_SIMULATIONS=1000,  # Lower for tests
        STRESS_SCENARIO_PCT_DROP=[Decimal("-0.05"), Decimal("-0.10"), Decimal("-0.15"), Decimal("-0.20")],
        VAR_ENGINE_TIMEOUT_SECONDS=2.0,
        VAR_ENGINE_FALLBACK_ON_TIMEOUT=True,
    )

@pytest.fixture
def position_limit_settings() -> PositionLimitSettings:
    return PositionLimitSettings(
        MAX_NIFTY_LOTS=5,
        MAX_BANKNIFTY_LOTS=3,
        MAX_MCX_GOLD_LOTS=2,
        MAX_MCX_SILVER_LOTS=2,
        MAX_MCX_CRUDEOIL_LOTS=2,
        NIFTY_LOT_SIZE=25,
        BANKNIFTY_LOT_SIZE=15,
        MAX_GREEK_DELTA_EXPOSURE=Decimal("5000"),
        MAX_GREEK_GAMMA_EXPOSURE=Decimal("500"),
        MAX_GREEK_VEGA_EXPOSURE=Decimal("50000"),
        MAX_GREEK_THETA_EXPOSURE=Decimal("3000"),
    )

@pytest.fixture
def var_engine(quant_risk_settings, position_limit_settings, risk_settings, audit_logger) -> RiskEngineOrchestrator:
    return RiskEngineOrchestrator(quant_risk_settings, position_limit_settings, risk_settings, audit_logger)

@pytest.fixture
def self_trade_prevention(audit_logger) -> SelfTradePrevention:
    return SelfTradePrevention(audit_logger)

@pytest.fixture
def order_validator(compliance_settings, broker_settings, position_limit_settings, audit_logger) -> OrderValidator:
    return OrderValidator(compliance_settings, broker_settings, position_limit_settings, audit_logger)

@pytest.fixture
def risk_manager_full(
    compliance_settings, risk_settings, kill_switch,
    quant_risk_settings, position_limit_settings,
    var_engine, self_trade_prevention, order_validator,
) -> RiskManager:
    return RiskManager(
        settings=compliance_settings,
        risk_settings=risk_settings,
        kill_switch=kill_switch,
        quant_settings=quant_risk_settings,
        position_settings=position_limit_settings,
        var_engine=var_engine,
        self_trade_prevention=self_trade_prevention,
        order_validator=order_validator,
    )

@pytest.fixture
def sample_pnl_series() -> pd.Series:
    """252 days of synthetic P&L data (normal distribution, mean=0, std=1000)."""
    rng = np.random.default_rng(42)
    return pd.Series(rng.normal(0, 1000, 252))

@pytest.fixture
def sample_return_series() -> pd.Series:
    """252 days of synthetic daily returns (normal, mean=0.0003, std=0.015)."""
    rng = np.random.default_rng(42)
    return pd.Series(rng.normal(0.0003, 0.015, 252))

@pytest.fixture
def risk_monitor(risk_manager_full, kill_switch, var_engine, audit_logger) -> RiskMonitor:
    return RiskMonitor(risk_manager_full, kill_switch, var_engine, audit_logger)

@pytest.fixture
def risk_api_client(risk_manager_full, kill_switch, var_engine, risk_monitor):
    """FastAPI TestClient for risk_api endpoints."""
    from fastapi.testclient import TestClient
    # Wire up app with test dependencies
    ...
    return TestClient(app)
```

---

## Instruction 18 — Update `pyproject.toml` — Add New Dependencies and Markers

### 18.1 Add to [project] dependencies:

```toml
"scipy>=1.12",
"statsmodels>=0.14",
"arch>=7.0",
"fastapi>=0.110",
"uvicorn>=0.29",
```

### 18.2 Add to [project.optional-dependencies] dev:

```toml
"httpx>=0.25",  # For FastAPI TestClient
```

### 18.3 Add to [tool.pytest.ini_options] markers:

```toml
markers = [
    "slow: slow tests",
    "integration: requires broker API connection",
    "paper_trade: paper trading mode tests",
    "var: VaR/CVaR engine tests",
    "garch: GARCH model tests",
    "evt: Extreme Value Theory tests",
    "stress: stress test engine tests",
    "stp: self-trade prevention tests",
    "bench: benchmark tests",
    "load: load tests (Locust)",
    "chaos: chaos/replay tests with simulated failures",
]
```

---

## Instruction 19 — Verification Commands (Run After All Instructions Complete)

```powershell
# 1. Install new dependencies
pip install scipy statsmodels arch fastapi uvicorn httpx

# 2. Verify imports
python -c "from src.risk.var_engine import RiskEngineOrchestrator; print('VaR engine OK')"
python -c "from src.risk.self_trade_prevention import SelfTradePrevention; print('STP OK')"
python -c "from src.risk.order_validator import OrderValidator; print('Order validator OK')"
python -c "from src.risk.risk_monitor import RiskMonitor; print('Risk monitor OK')"
python -c "from src.risk.risk_api import app; print('Risk API OK')"

# 3. Tier 0 — Every PR gate
ruff check src/ tests/
ruff format --check src/ tests/
mypy src/ --strict
bandit -c pyproject.toml -r src/
pytest tests/risk/test_var_engine.py tests/risk/test_self_trade_prevention.py tests/risk/test_order_validator.py tests/risk/test_risk_monitor.py tests/risk/test_risk_api.py -v --cov=src --cov-branch --cov-fail-under=80
pip-audit

# 4. Tier 1 — PR + nightly
pytest tests/property/ -v -m "var or garch or evt or stp"
pytest tests/replay/ -v -m chaos
pytest tests/bench/ --benchmark-only --benchmark-compare=prev
pytest tests/ -n auto -v

# 5. Tier 2 — Pre-release
trivy fs .
cyclonedx-bom -o sbom.json

# 6. Tier 3 — Load test
locust -f tests/load/locustfile_risk_api.py --host=http://localhost:8000 --users=10 --spawn-rate=1 --run-time=5m --headless

# 7. Integration tests (requires Docker: TimescaleDB + Redis)
docker-compose -f deployment/docker-compose.yml up -d
pytest tests/integration/ -v
docker-compose -f deployment/docker-compose.yml down
```

**Gate criteria (same as Phase 0):**
- ruff = 0 errors
- mypy = 0 errors (strict mode)
- bandit = 0 high-severity findings
- coverage >= 80% (branch coverage)
- pip-audit = 0 known vulnerabilities
- gitleaks = 0 secrets detected

---

## Cross-Reference Validation

| Item | Status | Notes |
|------|--------|-------|
| 500ms resting time | **REMOVED** | Replaced by `order_modification_limit_check()` (25 mods/order per Zerodha). NO resting time check anywhere. |
| Algo ID tagging | **CLARIFIED** | `format_algo_tag` is for Kite `tag` field (our audit); exchange algo ID = broker's job per SEBI Feb 2025 circular |
| OPS threshold | **VERIFIED** | 10 OPS per NSE/INVG/67858; check 15 = informational WARNING only |
| Position limits | **CLARIFIED** | 5 NIFTY / 3 BANKNIFTY lots are self-imposed (NOT SEBI mandates). PositionLimitSettings with SEBI reference for exposure limits. |
| Self-trade prevention | **DEFENSE-IN-DEPTH** | Client-side STP is supplementary; Zerodha RMS is primary enforcement |
| VaR engine | **SUPPLEMENTARY** | Not SEBI-mandated for retail; if unavailable, log WARNING and PASS check 11 |
| GARCH | **SUPPLEMENTARY** | Best-effort conditional volatility; if fit fails, fall back to historical VaR |
| EVT | **SUPPLEMENTARY** | Tail risk monitoring only; if insufficient exceedances, log WARNING and skip |
| Kill switch | **3-LEVEL ESCALATION** | THROTTLE → PAUSE → KILL per MiFID II Art.17, NIST RS.RP-1, ISO A.8.26 |
| Audit trail | **DUAL-WRITE** | TimescaleDB + JSONL file; 7-year retention; append-only DB triggers |
| Fail-closed principle | **ENFORCED** | Primary checks (1-10, 14) fail on error; supplementary checks (11-13, 15) degrade gracefully with WARNING |
| SEBI circular refs | **EVERY FUNCTION** | Each compliance rule references specific circular number |
| McNeil alignment | **VERIFIED** | Ch.2 (VaR/CVaR), Ch.4 (GARCH), Ch.5 (EVT/GPD), Ch.9 (stress testing) |
| NISM alignment | **VERIFIED** | SEBI regulatory framework coverage: CIR/MRD/DP/09/2012, Feb 2025 circular |
| Windows 11 compat | **VERIFIED** | All dependencies (scipy, statsmodels, arch) native Windows; no WSL-only tools |
| Existing Phase 0 | **PRESERVED** | All 10 original checks, classes, and tests unchanged; Phase 10 only EXTENDS |

---

## SEBI Circular Reference Card (Phase 10 Specific)

| Topic | Circular | What it means for Phase 10 code |
|-------|----------|--------------------------------|
| Pre-trade risk controls | CIR/MRD/DP/09/2012 | Checks 1-10 (Phase 0) + checks 11-15 (Phase 10) = comprehensive pre-trade pipeline |
| 500ms resting | PROPOSED 2016, DROPPED 2018 | **NOT IMPLEMENTED** — replaced by order_modification_limit_check (25 mods/order) |
| OPS registration | SEBI/HO/MIRSD/MIRSD-PoD/P/CIR/2025/0000013 + NSE/INVG/67858 | Check 15: informational WARNING if OPS > 10 |
| Kill switch escalation | MiFID II Art.17 + NIST RS.RP-1 | auto_escalate: THROTTLE→PAUSE→KILL |
| Audit trail | MiFID II Art.25(1) + NIST AU-9 + ISO A.8.15 | Dual-write (file + TimescaleDB), SHA-256 checksums, 7-year retention |
| VaR/CVaR | McNeil Ch.2-5 (industry standard) | Supplementary risk measure; not SEBI-mandated for retail |
| Stress testing | McNeil Ch.9 (scenario-based) | Best practice; 5%/10%/15%/20% portfolio drops |
| Self-trade prevention | CIR/MRD/DP/09/2012 + FIX STP | Client-side defense-in-depth; Zerodha RMS is primary |
| Greek limits | McNeil Ch.9 (sensitivity limits) | Self-imposed; not SEBI-mandated for retail |

---

## File Creation Summary (Phase 10)

| File | Action | Lines (est.) |
|------|--------|-------------|
| `config/settings.py` | EXTEND (add 2 classes) | +80 |
| `src/risk/var_engine.py` | CREATE | ~600 |
| `src/risk/self_trade_prevention.py` | CREATE | ~120 |
| `src/risk/order_validator.py` | CREATE | ~200 |
| `src/risk/risk_monitor.py` | CREATE | ~180 |
| `src/risk/risk_api.py` | CREATE | ~250 |
| `src/risk/manager.py` | EXTEND (add 5 checks + constructor params) | +150 |
| `src/risk/kill_switch.py` | EXTEND (add 2 methods) | +80 |
| `src/risk/audit.py` | EXTEND (add dual-write + event types) | +100 |
| `deployment/init.sql` | EXTEND (add 2 hypertables) | +80 |
| `tests/risk/test_var_engine.py` | CREATE | ~300 |
| `tests/risk/test_self_trade_prevention.py` | CREATE | ~100 |
| `tests/risk/test_order_validator.py` | CREATE | ~120 |
| `tests/risk/test_risk_monitor.py` | CREATE | ~80 |
| `tests/risk/test_risk_api.py` | CREATE | ~100 |
| `tests/property/test_var_properties.py` | CREATE | ~150 |
| `tests/property/test_risk_invariants.py` | CREATE | ~120 |
| `tests/property/test_self_trade_properties.py` | CREATE | ~60 |
| `tests/replay/test_risk_under_chaos.py` | CREATE | ~200 |
| `tests/integration/test_risk_full_pipeline.py` | CREATE | ~250 |
| `tests/bench/test_var_benchmark.py` | CREATE | ~100 |
| `tests/load/locustfile_risk_api.py` | CREATE | ~60 |
| `tests/conftest.py` | EXTEND (add Phase 10 fixtures) | +80 |
| `pyproject.toml` | EXTEND (add deps + markers) | +20 |
| **Total** | | **~3,440 lines** |

---

## Execution Order Summary

| Step | Instruction | Dependency |
|------|-------------|-----------|
| 1 | Add QuantitativeRiskSettings + PositionLimitSettings to config/settings.py | None |
| 2 | Create src/risk/var_engine.py | Step 1 (settings) |
| 3 | Create src/risk/self_trade_prevention.py | Phase 0 (audit.py) |
| 4 | Create src/risk/order_validator.py | Step 1 (settings), Phase 0 (compliance.py) |
| 5 | Extend src/risk/manager.py (15 checks) | Steps 2,3,4 |
| 6 | Extend src/risk/kill_switch.py (escalation) | Phase 0 (kill_switch.py) |
| 7 | Extend src/risk/audit.py (dual-write) | Phase 0 (audit.py) |
| 8 | Extend deployment/init.sql (2 hypertables) | None |
| 9 | Create src/risk/risk_monitor.py | Steps 5,6,2 |
| 10 | Create src/risk/risk_api.py | Steps 5,6,9 |
| 11 | Write unit tests (Instruction 11) | Steps 2,3,4,9,10 |
| 12 | Write property tests (Instruction 12) | Steps 2,3,5 |
| 13 | Write chaos/replay tests (Instruction 13) | Steps 5,2,6,7 |
| 14 | Write integration tests (Instruction 14) | Steps 5-10 |
| 15 | Write benchmark tests (Instruction 15) | Steps 2,5 |
| 16 | Write load tests (Instruction 16) | Step 10 |
| 17 | Update conftest.py (Instruction 17) | Steps 2-10 |
| 18 | Update pyproject.toml (Instruction 18) | None |
| 19 | Run full verification (Instruction 19) | All above |
