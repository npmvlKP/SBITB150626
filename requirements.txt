# SBITB-150626 Production Dependencies
# Generated from pyproject.toml - DO NOT EDIT MANUALLY
# Use: pip install -r requirements.txt --generate-hashes > requirements.txt
#
# CRITICAL FIXES (per /sbitb150626 strict protocol):
# 1. vollib>=1.0.11 (NOT py_vollib - deprecated)
# 2. newspaper4k>=0.9.2 (NOT newspaper3k - deprecated since 2020)
# 3. psycopg[binary]>=3.1 (NOT psycopg2-binary - version conflict)
# 4. All Python 3.12 (cp312) compatible

# Core build
setuptools>=61.0
wheel>=0.40.0

# Core data processing
numpy>=1.26.0
pandas>=2.2.0
polars>=1.0.0

# Technical Analysis
TA-Lib>=0.6.8  # Windows cp312 wheel already installed
ta>=0.11.0     # Pure Python backup indicators

# Options Greeks (fixed: vollib is canonical)
vollib>=1.0.11  # Canonical - import as vollib.*
QuantLib>=1.36   # Indian calendar, day-count conventions

# Data sources
kiteconnect>=4.2.0
jugaad-data>=0.20.0

# Database (fixed: psycopg3 async)
psycopg[binary]>=3.1

# Cache/Message Queue
redis>=5.0.0

# News/Sentiment (fixed: newspaper4k)
newspaper4k>=0.9.2
feedparser>=6.0.0
httpx>=0.27.0

# Logging
structlog>=24.0.0

# Time handling
python-dateutil>=2.8.0
pytz>=2024.1

# Utilities
orjson>=3.10.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Development dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-xdist>=3.5.0
pytest-timeout>=2.2.0
pytest-randomly>=3.15.0
hypothesis>=6.100.0
ruff>=0.5.0
mypy>=1.10.0
bandit>=1.7.9
pip-audit>=2.7.0
safety>=3.0.0
pre-commit>=3.6.0
